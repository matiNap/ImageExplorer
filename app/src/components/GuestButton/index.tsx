import React from "react";
import { Button } from "@material-ui/core";
import "./style.css";

export default () => {
  return (
    <Button variant="outlined" color="primary" className="auth-button">
      Guest
    </Button>
  );
};
